<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.tramshedtech.eventmanagement.mapper.EventMapper">

    <!-- Select all events -->
    <select id="getAllEvents" resultType="com.tramshedtech.eventmanagement.entity.Event">
        SELECT * FROM events WHERE del = 1
    </select>

    <!-- Select event by ID -->
    <select id="getEventById" parameterType="long" resultType="com.tramshedtech.eventmanagement.entity.Event">
        SELECT * FROM events WHERE id = #{id}
    </select>

    <!--Insert data into DB -->
    <insert id="add" useGeneratedKeys="true" keyProperty="id">
        INSERT INTO events(title, date, time, location, del, room, speaker, responsible, type, staff, company, contact, email, note, tag, cateringType, cateringCount, beverageType, dietaryRequirements, cateringServingTime)
        VALUES (#{title}, #{date}, #{time}, #{location}, 1, #{room}, #{speaker}, #{responsible}, #{type}, #{staff}, #{company}, #{contact}, #{email}, #{note}, #{tag}, #{cateringType}, #{cateringCount}, #{beverageType}, #{dietaryRequirements}, #{cateringServingTime})
    </insert>

    <!-- Delete event in DB -->
    <update id="softDelete">
        UPDATE events SET del = 0 WHERE id = #{id}
    </update>

    <!-- Update event data in DB -->
    <update id="update">
        UPDATE events
        SET title = #{title},
            date = #{date},
            time = #{time},
            room = #{room},
            type = #{type},
            speaker = #{speaker},
            tag = #{tag},
            responsible = #{responsible},
            staff = #{staff},
            company = #{company},
            contact = #{contact},
            email = #{email},
            note = #{note}
        WHERE id = #{id}
    </update>

    <!-- Select room name in DB -->
    <select id="getRoomById" resultType="string">
        SELECT CONCAT(b.room_name, ' ', b.location, ' ', b.postcode) AS fullAddress From events e
        JOIN bookings b ON e.room = b.id
        WHERE e.id = #{eventId}
    </select>
</mapper>
